@using empty_cms.Utils;
@model OtherCardListBlock

<div>@Html.PropertyFor(x => x.Heading)</div>
<section class="d-flex flex-wrap gap-3 justify-content-center">
    @Html.PropertyFor(x => x.Cards, new { HasContainer = false })
</section>